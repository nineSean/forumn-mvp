# Infrastructure CDK (Tasks 9, 11)

- [x] Complete Task 9: Create packages/infra with package.json, tsconfig.json, cdk.json, bin/app.ts (CDK entry), lib/network-stack.ts (VPC), lib/database-stack.ts (RDS PostgreSQL db.t4g.micro + RDS Proxy + Secrets Manager), lib/api-stack.ts (Lambda + API Gateway HTTP API), install deps, commit — all files created and deps installed
- [x] Complete Task 11: Create .github/workflows/deploy-api.yml (GitHub Actions: checkout, pnpm install, test, prisma generate, build, cdk deploy), commit — workflow file created
