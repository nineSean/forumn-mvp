# Backend API (Tasks 3, 4, 5, 12)

- [x] Complete Task 3: Create packages/api with package.json, tsconfig.json, prisma/schema.prisma, src/lib/prisma.ts, src/lib/errors.ts, install deps, generate Prisma client, commit — files existed from prior attempt, deps installed, Prisma generated
- [x] Complete Task 4: Create GraphQL schema (src/graphql/schema.ts), board resolvers, post resolvers, yoga factory, test helpers, board tests, post tests, run tests, commit — all 7 tests pass, used makeExecutableSchema + maskedErrors:false for test yoga
- [x] Complete Task 5: Create reply resolvers, user resolvers, resolver index, auth middleware, Hono app entry (src/index.ts), reply tests, user tests (me query), run tests, commit — all 10 tests pass
- [x] Complete Task 12: Add login query to schema, implement login resolver with bcrypt, write login tests, run tests, commit — all 12 tests pass
